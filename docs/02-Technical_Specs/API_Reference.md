# API Error Codes - أكواد الأخطاء

## 📋 نظرة عامة

هذا الملف يوثق جميع أكواد الأخطاء المستخدمة في الـ API.

---

## 🔢 HTTP Status Codes

| Code | المعنى | الاستخدام |
|------|--------|----------|
| `200` | OK | نجاح العملية |
| `201` | Created | تم الإنشاء بنجاح |
| `204` | No Content | تم الحذف/الإلغاء بنجاح |
| `400` | Bad Request | بيانات غير صحيحة |
| `401` | Unauthorized | غير مُسجل دخول |
| `403` | Forbidden | غير مصرح |
| `404` | Not Found | غير موجود |
| `422` | Validation Error | خطأ في البيانات |
| `429` | Too Many Requests | تجاوز حد الطلبات |
| `500` | Server Error | خطأ في السيرفر |

---

## 📑 أكواد الأخطاء المخصصة

### هيكل الاستجابة
```json
{
    "success": false,
    "error": {
        "code": "INV_001",
        "message": "رسالة الخطأ بالعربية",
        "message_en": "Error message in English",
        "details": {}
    }
}
```

---

## 📄 أخطاء الفواتير (INV)

| الكود | الرسالة | السبب |
|-------|---------|-------|
| `INV_001` | لا يمكن حذف الفواتير | الحذف ممنوع، استخدم الإلغاء |
| `INV_002` | لا يمكن تقليل القيمة أقل من المدفوع | `new_total < paid_amount` |
| `INV_003` | لا يمكن إعادة تفعيل فاتورة ملغاة | محاولة تغيير status من cancelled |
| `INV_004` | الفاتورة خارج نافذة التعديل | تجاوز `edit_window_days` |
| `INV_005` | الكمية المطلوبة غير متوفرة | FIFO check failed |
| `INV_006` | الفاتورة يجب أن تحتوي على بند واحد على الأقل | `items.length = 0` |
| `INV_008` | لا يمكن إلغاء فاتورة مدفوعة | `paid_amount > 0` |

---

## 💰 أخطاء التحصيلات (COL)

| الكود | الرسالة | السبب |
|-------|---------|-------|
| `COL_001` | لا يمكن حذف التحصيلات | الحذف ممنوع |
| `COL_002` | المبلغ يجب أن يكون أكبر من صفر | `amount <= 0` |
| `COL_003` | الفاتورة لا تخص هذا العميل | Manual allocation error |
| `COL_004` | التحصيل خارج نافذة التعديل | تجاوز `edit_window_days` |
| `COL_005` | إجمالي التوزيع أكبر من مبلغ التحصيل | Over-allocation |
| `COL_006` | المبلغ أكبر من رصيد الفاتورة | Allocation exceeds balance |

---

## 📦 أخطاء المرتجعات (RET)

| الكود | الرسالة | السبب |
|-------|---------|-------|
| `RET_001` | لا توجد شحنة مفتوحة لاستقبال المرتجع | No open shipment |

---

## 📦 أخطاء الشحنات (SHP)

| الكود | الرسالة | السبب |
|-------|---------|-------|
| `SHP_001` | لا يمكن حذف شحنة لها فواتير مرتبطة | FK constraint |
| `SHP_002` | لا يمكن تعديل شحنة مُصفاة | `status = settled` |
| `SHP_003` | الشحنة مُصفاة بالفعل | محاولة settle مرتين |
| `SHP_004` | الشحنة التالية يجب أن تكون مفتوحة | Selected shipment not open |
| `SHP_005` | لا يمكن إلغاء التصفية - الكمية تم بيعها | Unsettle safety check |
| `SHP_009` | لا يمكن تعديل شحنة غير مفتوحة | Update only for open shipments |
| `SHP_010` | لا يمكن تقليل الكمية أقل من المباع | `new_qty < sold_qty` |
| `SHP_006` | لا يمكن ترحيل الشحنة لنفسها | Self carryover |
| `SHP_007` | الشحنة ليست مُصفاة | Unsettle on non-settled |

---

## 🔐 أخطاء المصادقة (AUTH)

| الكود | الرسالة | السبب |
|-------|---------|-------|
| `AUTH_001` | غير مُسجل دخول | Token missing/expired |
| `AUTH_002` | غير مصرح بهذه العملية | No permission |
| `AUTH_003` | الحساب مقفل | `is_locked = true` |
| `AUTH_004` | فشل تسجيل الدخول | Invalid credentials |
| `AUTH_005` | تجاوز عدد المحاولات المسموحة | Failed attempts limit |

---

## ⚙️ أخطاء النظام (SYS)

| الكود | الرسالة | السبب |
|-------|---------|-------|
| `SYS_001` | خطأ في الخادم | Uncaught exception |
| `SYS_002` | تجاوز حد الطلبات | Rate limit exceeded |
| `SYS_003` | الخدمة غير متاحة مؤقتاً | Maintenance mode |

---

## 🏠 Endpoints إضافية (Additional)

### Health Check
| Method | Endpoint | الوصف |
|--------|----------|-------|
| `GET` | `/api/health` | فحص حالة الـ API |

### Dashboard
| Method | Endpoint | الوصف |
|--------|----------|-------|
| `GET` | `/api/dashboard` | ملخص لوحة التحكم |
| `GET` | `/api/dashboard/activity` | آخر النشاطات |

### Treasury (الخزنة والبنك)
| Method | Endpoint | الوصف |
|--------|----------|-------|
| `GET` | `/api/cashbox` | رصيد الخزنة |
| `GET` | `/api/cashbox/transactions` | حركات الخزنة |
| `POST` | `/api/cashbox/deposit` | إيداع للخزنة |
| `POST` | `/api/cashbox/withdraw` | سحب من الخزنة |
| `GET` | `/api/bank` | رصيد البنك |
| `GET` | `/api/bank/transactions` | حركات البنك |
| `POST` | `/api/bank/deposit` | إيداع للبنك |
| `POST` | `/api/bank/withdraw` | سحب من البنك |

### Accounts & Transfers
| Method | Endpoint | الوصف |
|--------|----------|-------|
| `GET` | `/api/accounts` | قائمة الحسابات |
| `GET` | `/api/accounts/summary` | ملخص الأرصدة |
| `GET/POST` | `/api/transfers` | التحويلات |

### Shipments (الشحنات)
| Method | Endpoint | الوصف |
|--------|----------|-------|
| `GET` | `/api/shipments` | قائمة الشحنات |
| `POST` | `/api/shipments` | إنشاء شحنة جديدة |
| `GET` | `/api/shipments/{id}` | تفاصيل الشحنة |
| `PUT` | `/api/shipments/{id}` | تعديل شحنة (open فقط) |
| `DELETE` | `/api/shipments/{id}` | حذف شحنة |
| `POST` | `/api/shipments/{id}/close` | إغلاق شحنة |
| `POST` | `/api/shipments/{id}/settle` | تصفية شحنة |
| `POST` | `/api/shipments/{id}/unsettle` | إلغاء تصفية |
| `GET` | `/api/shipments/stock` | المخزون المتاح للبيع |
| `GET` | `/api/shipments/{id}/settlement-report` | تقرير التصفية |

### Inventory
| Method | Endpoint | الوصف |
|--------|----------|-------|
| `GET` | `/api/permissions` | قائمة الصلاحيات المتاحة |

---

## 📊 Rate Limiting

| Endpoint | الحد | النافذة |
|----------|------|---------|
| `/api/auth/*` | 10 requests | per minute |
| `/api/*` (authenticated) | 60 requests | per minute |
| `/api/reports/*` | 10 requests | per minute |

### Response Headers
```
X-RateLimit-Limit: 60
X-RateLimit-Remaining: 45
X-RateLimit-Reset: 1702425600
```

---

## 🔄 Retry Logic

للأخطاء القابلة للإعادة:
```javascript
const retryableErrors = [429, 500, 502, 503, 504];

async function fetchWithRetry(url, options, maxRetries = 3) {
    for (let i = 0; i < maxRetries; i++) {
        try {
            const response = await fetch(url, options);
            if (!retryableErrors.includes(response.status)) {
                return response;
            }
            await delay(Math.pow(2, i) * 1000); // Exponential backoff
        } catch (error) {
            if (i === maxRetries - 1) throw error;
        }
    }
}
```

---

## 🌐 Localization

الأخطاء تُرجع بلغتين:
```json
{
    "error": {
        "code": "INV_002",
        "message": "لا يمكن تقليل القيمة أقل من المدفوع",
        "message_en": "Cannot reduce total below paid amount"
    }
}
```

استخدام الـ Header:
```
Accept-Language: ar  // العربية
Accept-Language: en  // English
```
